# Build Steward — Python dependencies
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Migrate>=4.0,<5.0
SQLAlchemy>=2.0,<3.0
APScheduler>=3.10,<4.0
requests>=2.31
python-dateutil>=2.9
# NB: no yfinance. Its only use was one price lookup, now a plain requests
# call in services/market.py -- it dragged in pandas + numpy, the only
# compiled dependencies here, which are slow and failure-prone to build on
# Android/Termux. Everything above is pure Python.

# Dev / tests
pytest>=8.0
